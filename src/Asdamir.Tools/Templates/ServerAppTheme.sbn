@* Generated by `asdamir new app {{ AppName }}` on {{ GeneratedAtUtc }}. *@
@* AppTheme — thin app-local alias that delegates to the framework's AsdamirThemeProvider facade.
   The skin→accent map + light/dark logic (and the underlying design-system component) live inside
   the facade, so this file — and the layouts that render <AppTheme /> — never reference FluentUI. *@
<AsdamirThemeProvider />
