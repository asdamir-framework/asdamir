/* Generated by `asdamir new app {{ AppName }}` on {{ GeneratedAtUtc }}.
   ─────────────────────────────────────────────────────────────────────────────
   This app's per-app style OVERRIDES only. The full INSPINIA "Pixel" theme —
   the palette/tokens (`:root`, `--asd-*`), the `.asd-*` design system (buttons,
   tables, panels, pills, toolbars), self-hosted Inter fonts, global FluentUI
   element theming, and the `html[data-skin]` skins — lives ONCE in the
   Asdamir.Web package and is served as `_content/Asdamir.Web/asdamir-theme.css`
   (linked in App.razor BEFORE this file). Every Asdamir app shares that ONE
   stylesheet; it is NEVER copied per app (that is exactly what used to drift).

   Add ONLY genuine app-specific overrides below — e.g. a different brand accent:
       :root { --asd-accent: #8e7fe0; --asd-accent-hover: #786bbe; }
   Keep the `.asd-*` design system in the shared theme; do not re-declare it here.
   No inline `style=` anywhere (audit-lint AUD013). ─────────────────────────── */
